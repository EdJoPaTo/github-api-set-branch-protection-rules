{
	"private": true,
	"license": "MIT",
	"scripts": {
		"reorder-repos-in-folders": "tsc --sourceMap && node --enable-source-maps dist/reorder-repos-in-folders.js",
		"update-repos": "tsc --sourceMap && node --enable-source-maps dist/update-repos.js",
		"test": "tsc --sourceMap && xo"
	},
	"type": "module",
	"engines": {
		"node": ">=16"
	},
	"dependencies": {
		"@octokit/core": "^4.0.4",
		"array-filter-unique": "^3.0.0"
	},
	"devDependencies": {
		"@sindresorhus/tsconfig": "^3.0.0",
		"@types/node": "^16.0.0",
		"typescript": "^4.2.3",
		"xo": "^0.52.3"
	},
	"xo": {
		"rules": {
			"@typescript-eslint/naming-convention": "off",
			"no-await-in-loop": "off",
			"unicorn/no-await-expression-member": "off"
		}
	}
}
